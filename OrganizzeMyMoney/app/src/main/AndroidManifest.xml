<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.organizzemymoney">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.areaPrincipal.Activity_Receitas"
            android:theme="@style/receitasTheme" />
        <activity
            android:name=".activity.areaPrincipal.Activity_Despesas"
            android:theme="@style/despesasTheme" />
        <activity
            android:name=".activity.areaPrincipal.MainActivity_principal"
            android:label="@string/title_activity_main_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.intro.CadastroActivity" />
        <activity android:name=".activity.intro.LoginActivity" />
        <activity
            android:name=".activity.intro.MainActivity"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>